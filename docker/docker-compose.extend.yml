version: '3.8'

services:
    app:
        ports:
            - '8000:8000'
        volumes:
            - .:/workspace
            - /workspace/node_modules
            - /workspace/.parcel-cache
            - /workspace/__pycache__
    db:
        ports:
            - '3306'